
def main():

    a = input("Enter first number: ")
    b = input("Enter second number: ")
    sum_ab = float(a) + float(b)
    substract_ab = float(a) - float(b)
    multiply_ab = float(a) * float(b)
    divide_ab = float(a) / float(b)
    floor_divide_ab = float(a) // float(b)
    modulus_ab = float(a) % float(b)
    exponent_ab = float(a) ** float(b)
    print("The difference of", a, "and", b, "is:", substract_ab)
    print("The product of", a, "and", b, "is:", multiply_ab)
    print("The division of", a, "by", b, "is:", divide_ab)
    print("The floor division of", a, "by", b, "is:", floor_divide_ab)
    print("The modulus of", a, "and", b, "is:", modulus_ab)
    print("The", a, "raised to the power of", b, "is:", exponent_ab)
    print("The sum of", a, "and", b, "is:", sum_ab)
    
if __name__ == "__main__":
    main()
    
    



























if __name__ == "__main__":
    main()
